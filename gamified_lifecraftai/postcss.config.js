module.exports = {
  plugins: {
    // This tells PostCSS to use the Tailwind CSS plugin
    tailwindcss: {},
    // This adds vendor prefixes (like -webkit-, -moz-) for better browser compatibility
    autoprefixer: {},
  },
}